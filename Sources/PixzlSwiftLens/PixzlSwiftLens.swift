/// PixzlSwiftLens — drop-in SwiftUI debug HUD for iOS apps.
///
/// Add `.pixzlSwiftLens()` to your root view, shake the device to expand.
/// In Release builds the modifier compiles down to a no-op.
public enum PixzlSwiftLens {
    public static let version = "0.1.0"
}
